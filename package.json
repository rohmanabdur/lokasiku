{
  "name": "lokasiku",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@maptiler/leaflet-maptilersdk": "^4.1.1",
    "leaflet": "^1.9.4"
  }
}
